# Prisma CLI / seed config — copy this file to `.env`.
# Should match the backend DATABASE_URL; used by `prisma migrate` and `db:seed:*`.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require